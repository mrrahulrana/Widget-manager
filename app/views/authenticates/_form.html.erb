<%= form_with(model: authenticate, local: true) do |form| %>
  <% if authenticate.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(authenticate.errors.count, "error") %> prohibited this authenticate from being saved:</h2>

      <ul>
      <% authenticate.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :email %>
    <%= form.text_field :email, id: :authenticate_email %>
  </div>

  <div class="field">
    <%= form.label :password %>
    <%= form.text_field :password, id: :authenticate_password %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
